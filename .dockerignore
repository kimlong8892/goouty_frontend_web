node_modules
.git
.github
.gitignore
.idea
.vscode
dist
build
coverage
*.local
*.log
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun.lockb
!bun.lockb
# Keep bun.lockb but usually we copy it explicitly before COPY . .
# So strictly speaking we can ignore it in COPY . . if we want, but usually it's fine.
# But definitely ignore node_modules and .git
